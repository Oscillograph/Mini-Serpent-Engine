#include <mse/systems/platform/events/events.h>

// THIS SYSTEM IS UNUSED

namespace mse
{
	/*
	Event::Event() 
	{
	}
	
	Event::Event(SDL_Event* e)
	{
		Process(e);
	}
	
	Event::~Event()
	{
	}
	
	void Event::Process(SDL_Event* e)
	{
		type = (EventTypes)(e->type);
		if (type == EventTypes::Window)
		{
			event = (EventSubtypes)(e->window.event);
			windowID = e->window.windowID;
		}
		
		if (type == EventTypes::MouseMoved)
		{
			windowID = e->motion.windowID;
			x = e->motion.x;
			y = e->motion.y;
		}
		
		if (type == EventTypes::MouseButtonDown)
		{
			windowID = e->window.windowID;
			button = e->key.keysym.scancode;
		}
	}
	*/
}
